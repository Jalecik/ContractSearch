{{>page/nav}}

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <img src="{{product.image}}" alt="">
        </div>
        <div class="col-md-8">
            <h1>{{product.name}}</h1>
        </div>
    </div>
</div>